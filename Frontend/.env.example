# Desarrollo local: apunta al backend FastAPI corriendo localmente
VITE_API_BASE_URL=http://127.0.0.1:8000

# Producción (mismo origen): dejar vacío para que el frontend use rutas relativas.
# El CI/CD del workflow de GitHub Actions establece VITE_API_BASE_URL=""
# antes de hacer el build, así /auth/login resuelve al mismo App Service.
# VITE_API_BASE_URL=
